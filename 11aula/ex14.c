#include<stdio.h>
#include<stdlib.h>

struct alunos
{
    int mat;
    char nome[60], dtn[11];
};


int main(){

    int n;

    scanf("%d" &n);

    struct alunos *alunos = (struct Aluno *)malloc(n * sizeof(struct Aluno));



    free(struct alunos)

    return 0;
}